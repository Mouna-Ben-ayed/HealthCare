<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthCare">
        <activity
            android:name=".Views.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Views.MedicineTimeActivity"
            android:exported="false" />
        <activity
            android:name=".Views.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".Views.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".Views.NewsActivity"
            android:exported="false" />
        <activity
            android:name=".Views.DoctorTimeActivity"
            android:exported="false" />
        <activity
            android:name=".Views.DoctorInfoActivity"
            android:exported="false" />
        <activity
            android:name=".Views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".Views.receivers.MedicineAlarmReceiver"
            android:exported="true" />

    </application>

</manifest>